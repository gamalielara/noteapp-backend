{
  "name": "note-app-backend",
  "version": "1.0.0",
  "description": "A simple note app backend application created using javascript",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 1991",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ara Gamaliel Boanerges",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "json-server": "^0.17.2"
  }
}
